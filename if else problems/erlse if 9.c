#include <stdio.h>
int main() {
    int num;
    scanf("%d",&num);
    if(num>100){
        printf("three digits number");
    } 
    else {
        printf("not three digits number");
    }

    
    return 0;
}